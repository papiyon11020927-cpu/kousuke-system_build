name: Build & Deploy to Firebase Hosting

on:
  push:
    branches:
      - main   # main ブランチへの push で自動実行

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest   # Linux 環境（Windows スタック問題なし）

    steps:
      - name: コードを取得
        uses: actions/checkout@v4

      - name: Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: 依存パッケージインストール
        working-directory: app
        run: |
          rm -f package-lock.json
          npm install

      - name: TypeScript チェック & ビルド
        working-directory: app
        run: npm run build
        env:
          VITE_FIREBASE_API_KEY:              ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN:          ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID:           ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET:       ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID:  ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID:               ${{ secrets.VITE_FIREBASE_APP_ID }}
          VITE_APP_ID:                        ${{ secrets.VITE_APP_ID }}
          VITE_GEMINI_API_KEY:                ${{ secrets.VITE_GEMINI_API_KEY }}

      - name: Firebase CLI インストール
        run: npm install -g firebase-tools

      - name: Firebase にデプロイ（Hosting + Storage ルール）
        run: firebase deploy --only hosting,storage --token ${{ secrets.FIREBASE_TOKEN }}
